// The shared package: types, DTO schemas, and constants used by BOTH web and api.
// Nothing backend-only (no Prisma/secrets) and nothing frontend-only (no React).
export * from "./health";
export * from "./client";
