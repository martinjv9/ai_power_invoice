{
  "name": "invoice-system",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w @invoice/api\" \"npm run dev -w @invoice/web\"",
    "test": "npm run test -w @invoice/api && npm run test -w @invoice/web",
    "build": "npm run build -w @invoice/shared && npm run build -w @invoice/api && npm run build -w @invoice/web",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2"
  }
}
