// Prisma schema. Models are added starting in App Phase 1 (User), then Phase 2+
// (Client, Project, Document, LineItem, Payment). Phase 0 just wires Prisma up.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
